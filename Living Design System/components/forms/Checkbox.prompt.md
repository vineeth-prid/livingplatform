Checkbox with optional label and description text. Pine fill when checked.

```jsx
<Checkbox label="Furnished only" defaultChecked />
<Checkbox label="Pet friendly" description="Cats and dogs welcome" />
```

Works controlled (`checked` + `onChange`) or uncontrolled (`defaultChecked`).
