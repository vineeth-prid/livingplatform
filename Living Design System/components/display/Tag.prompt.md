Filter / attribute chip. Selectable (Pine fill) and optionally removable.

```jsx
<Tag selected onClick={toggle}>2 BHK</Tag>
<Tag icon={<span>◎</span>}>Near metro</Tag>
<Tag removable onRemove={clear}>Furnished</Tag>
```

Use rows of Tags for filters and amenity lists.
