# pnpm configuration for the Living Platform monorepo
# NestJS + Prisma rely on hoisted binaries and postinstall scripts.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
# Prisma / argon2 / esbuild run postinstall build steps
enable-pre-post-scripts=true
